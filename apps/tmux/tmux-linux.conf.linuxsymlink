# This is for the tmux version < 2.0. When tmux is upgraded to 2.0 above, this can be removed.

# Setup 'v' to begin selection as in Vim
#bind-key -n vi-copy v begin-selection
#bind-key -n vi-copy y copy-pipe "xclip -in -selection clipboard"

# Update default binding of `Enter` to also use copy-pipe
#unbind -n vi-copy Enter
#bind-key -n vi-copy Enter copy-pipe "xclip -in -selection clipboard"
